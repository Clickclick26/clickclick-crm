VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional — real outbound calling is not wired into the dialer UI yet
# (see src/lib/telnyx/client.ts). Leave blank until you have a Telnyx
# account; the app works fine without it, same as today.
VITE_TELNYX_LOGIN_TOKEN=
